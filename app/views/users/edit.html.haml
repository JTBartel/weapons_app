%h1 
 = "Edit " + @user.name
= form_for(@user) do |f|
 = render 'shared/error_messages', object: f.object
 = render 'fields_for_edit', :f => f
%div
 = gravatar_for @user
 %h3
  = link_to "change", "http://gravatar.com/emails", :target => "_blank"